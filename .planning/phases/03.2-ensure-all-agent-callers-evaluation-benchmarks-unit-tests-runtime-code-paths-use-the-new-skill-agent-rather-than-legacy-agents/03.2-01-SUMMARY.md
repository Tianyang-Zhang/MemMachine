# 03.2-01 Summary

## Completed

- `evaluation/utils/agent_utils.py`
  - Retrieval mode now builds top-level runtime via `create_retrieval_agent(...)`.
  - Direct bypass mode still uses `MemMachineAgent`.
  - Metric aggregation now falls back across `selected_tool`, `selected_skill`,
    `route`, and `agent`.
- Retrieval benchmark script caller defaults switched from `ToolSelectAgent` to
  `RetrieveSkill`.

## Verification

- `.venv/bin/ruff check` on all modified evaluation caller files passed.
