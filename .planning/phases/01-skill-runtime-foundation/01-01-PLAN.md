---
phase: 01-skill-runtime-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memmachine/retrieval_agent/skills/__init__.py
  - src/memmachine/retrieval_agent/skills/types.py
  - src/memmachine/retrieval_agent/skills/spec_loader.py
  - src/memmachine/retrieval_agent/skills/runtime.py
  - tests/memmachine/retrieval_agent/test_skill_runtime.py
autonomous: true
requirements:
  - ORCH-01
user_setup: []
must_haves:
    truths:
      - Retrieval skill runtime contracts are explicit, strict, and importable by downstream skill modules.
      - Invalid skill outputs are normalized once and then fail with stable developer-facing contract errors.
      - Foundation tests verify strict required fields, unknown-field rejection, and single-v1 contract behavior.
    artifacts:
      - path: src/memmachine/retrieval_agent/skills/types.py
        provides: Canonical skill request/result/error contracts.
        min_lines: 40
      - path: src/memmachine/retrieval_agent/skills/runtime.py
        provides: Runtime validation/normalization helpers with contract error mapping.
        min_lines: 40
      - path: tests/memmachine/retrieval_agent/test_skill_runtime.py
        provides: Unit coverage for strict runtime contract behavior.
        min_lines: 40
    key_links:
      - from: src/memmachine/retrieval_agent/skills/runtime.py
        to: src/memmachine/retrieval_agent/skills/types.py
        via: Runtime uses shared contract models and typed error codes.
        pattern: from memmachine\.retrieval_agent\.skills\.types import
      - from: tests/memmachine/retrieval_agent/test_skill_runtime.py
        to: src/memmachine/retrieval_agent/skills/runtime.py
        via: Tests directly exercise runtime contract validation paths.
        pattern: from memmachine\.retrieval_agent\.skills\.runtime import
---

<objective>
Establish strict runtime contracts and validation primitives for skill-style
retrieval execution.

Purpose: Phase 1 requires a stable and strict foundation before orchestration
wiring.
Output: Typed contracts + runtime/spec loader helpers + baseline unit tests.
</objective>

<execution_context>
@/memverge/home/tomz/.codex/get-shit-done/workflows/execute-plan.md
@/memverge/home/tomz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-skill-runtime-foundation/01-RESEARCH.md

@src/memmachine/retrieval_agent/common/agent_api.py
@src/memmachine/retrieval_agent/service_locator.py
@tests/memmachine/retrieval_agent/test_retrieval_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define skill contract types and export surface</name>
  <files>src/memmachine/retrieval_agent/skills/types.py, src/memmachine/retrieval_agent/skills/__init__.py</files>
  <action>Create strict v1 skill contract models and typed contract error definitions with stable `SKILL_CONTRACT_*` codes. Ensure required fields are explicit and unknown fields are rejected. Export only canonical public symbols from `__init__.py` so downstream skills share one contract surface.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_skill_runtime.py -k "contract or schema"</automated></verify>
  <done>Contracts compile, required-field strictness is encoded, and public exports are available from `skills.__init__`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime/spec validation helpers</name>
  <files>src/memmachine/retrieval_agent/skills/spec_loader.py, src/memmachine/retrieval_agent/skills/runtime.py</files>
  <action>Implement strict skill spec loading and runtime normalization behavior: allow one normalization attempt and fail with typed developer-first errors if output remains invalid. Keep single v1 contract behavior only, include explicit fallback trigger reason fields in runtime error payloads, and do not add multi-version parsing in this phase.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_skill_runtime.py -k "normalize or invalid or error"</automated></verify>
  <done>Spec/runtime helpers enforce strict schema rules and produce deterministic typed errors with clear fix guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Add foundation unit tests for strict behavior</name>
  <files>tests/memmachine/retrieval_agent/test_skill_runtime.py</files>
  <action>Add unit tests that prove strict required-field handling, unknown-field rejection, one-pass normalization semantics, and stable error code emission. Cover success and failure paths so plan 2 can wire retrieval entry with confidence.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_skill_runtime.py</automated></verify>
  <done>Test suite validates Phase 1 contract/runtime invariants and passes reliably.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/memmachine/retrieval_agent/test_skill_runtime.py`
- [ ] `uv run ruff check src/memmachine/retrieval_agent/skills tests/memmachine/retrieval_agent/test_skill_runtime.py`
- [ ] Typed `SKILL_CONTRACT_*` errors are present and asserted in tests
</verification>

<success_criteria>
- All tasks completed
- Contract/runtime foundation is strict and deterministic
- Tests prove normalization + strict validation behavior
</success_criteria>

<output>
After completion, create .planning/phases/01-skill-runtime-foundation/01-01-SUMMARY.md
</output>
