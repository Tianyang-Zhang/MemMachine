---
phase: 01-skill-runtime-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/memmachine/retrieval_agent/skills/retrieve_skill.py
  - src/memmachine/retrieval_agent/service_locator.py
  - tests/memmachine/retrieval_agent/test_retrieval_agent.py
  - tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py
autonomous: true
requirements:
  - ORCH-01
user_setup: []
must_haves:
    truths:
      - Retrieval requests can enter a `retrieve-skill` orchestration path via service locator wiring.
      - Phase 1 keeps caller-facing retrieval response behavior compatible enough for current tests while prioritizing authoring convenience.
      - Bootstrap integration tests verify retrieve-skill entry path and guard against legacy-side effects.
    artifacts:
      - path: src/memmachine/retrieval_agent/skills/retrieve_skill.py
        provides: Retrieve-skill orchestration bootstrap class wired to Phase 1 runtime contracts.
        min_lines: 50
      - path: src/memmachine/retrieval_agent/service_locator.py
        provides: Retrieval agent factory wiring for retrieve-skill entry path.
        contains: create_retrieval_agent
      - path: tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py
        provides: Integration tests for service-locator bootstrap and basic query path behavior.
        min_lines: 40
    key_links:
      - from: src/memmachine/retrieval_agent/service_locator.py
        to: src/memmachine/retrieval_agent/skills/retrieve_skill.py
        via: Service locator imports and instantiates retrieve-skill path.
        pattern: RetrieveSkill
      - from: tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py
        to: src/memmachine/retrieval_agent/service_locator.py
        via: Tests call factory and assert retrieve-skill entry behavior.
        pattern: create_retrieval_agent
---

<objective>
Wire `retrieve-skill` bootstrap into retrieval-agent construction and verify entry
behavior with integration tests.

Purpose: Complete Phase 1 by making the new foundation reachable through the
existing retrieval entrypoint.
Output: retrieve-skill bootstrap module + service-locator wiring + integration tests.
</objective>

<execution_context>
@/memverge/home/tomz/.codex/get-shit-done/workflows/execute-plan.md
@/memverge/home/tomz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-skill-runtime-foundation/01-RESEARCH.md

@src/memmachine/retrieval_agent/service_locator.py
@src/memmachine/retrieval_agent/common/agent_api.py
@tests/memmachine/retrieval_agent/test_retrieval_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement retrieve-skill orchestration bootstrap</name>
  <files>src/memmachine/retrieval_agent/skills/retrieve_skill.py</files>
  <action>Create a Phase 1 bootstrap retrieval skill agent that uses the strict runtime contracts from plan 01, initializes execution context, and exposes developer-first error handling with explicit fallback trigger reasons. Keep scope to entry orchestration baseline; do not implement full split/coq orchestration in this phase.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py -k "bootstrap or errors"</automated></verify>
  <done>Retrieve-skill bootstrap exists with strict contract usage and deterministic error behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire retrieve-skill path into service locator</name>
  <files>src/memmachine/retrieval_agent/service_locator.py</files>
  <action>Extend `create_retrieval_agent` to support retrieve-skill bootstrap routing while preserving existing agent options. Keep external behavior stable where practical, but if strict conflict arises with convenience, prefer convenience per Phase 1 decision and cover the behavior in tests.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_retrieval_agent.py -k "service_locator or retrieval"</automated></verify>
  <done>Factory can construct retrieve-skill path without breaking existing baseline agent creation use cases.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage for retrieve-skill entry path</name>
  <files>tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py, tests/memmachine/retrieval_agent/test_retrieval_agent.py</files>
  <action>Add integration-level tests for service-locator retrieve-skill bootstrap, contract enforcement at entry, and compatibility expectations for caller-facing responses. Include assertions that fallback reason semantics are explicit in failure paths.</action>
  <verify><automated>uv run pytest tests/memmachine/retrieval_agent/test_retrieval_agent.py tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py</automated></verify>
  <done>Test suite confirms retrieval requests enter retrieve-skill path and Phase 1 guardrails hold.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/memmachine/retrieval_agent/test_retrieval_agent.py tests/memmachine/retrieval_agent/test_retrieve_skill_bootstrap.py`
- [ ] `uv run ruff check src/memmachine/retrieval_agent/service_locator.py src/memmachine/retrieval_agent/skills tests/memmachine/retrieval_agent`
- [ ] Service locator supports retrieve-skill bootstrap path with explicit tests
</verification>

<success_criteria>
- All tasks completed
- retrieve-skill bootstrap path is wired and test-covered
- Phase 1 entry-goal from roadmap is satisfied
</success_criteria>

<output>
After completion, create .planning/phases/01-skill-runtime-foundation/01-02-SUMMARY.md
</output>
