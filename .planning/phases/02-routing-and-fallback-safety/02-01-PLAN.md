---
phase: 02-routing-and-fallback-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memmachine/retrieval_agent/skills/route_policy.py
  - src/memmachine/retrieval_agent/skills/retrieve_skill.py
  - src/memmachine/retrieval_agent/skills/specs/top_level/retrieve_skill.md
  - src/memmachine/retrieval_agent/skills/types.py
  - tests/memmachine/retrieval_agent/test_route_policy.py
  - tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py
autonomous: true
requirements:
  - ROUT-01
  - SAFE-04
user_setup: []
must_haves:
    truths:
      - Top-level retrieval flow uses an explicit `select-skill` decision contract with a single selected route, confidence score, and reason metadata.
      - Selector behavior retries once on unclassifiable output and resolves conflicting retry results by choosing higher confidence.
      - Selector retry and decision logic remain bounded and policy-driven so phase safety controls can enforce max-step behavior.
    artifacts:
      - path: src/memmachine/retrieval_agent/skills/route_policy.py
        provides: Route decision contract models, parser, and retry/tie-break evaluator.
        min_lines: 60
      - path: src/memmachine/retrieval_agent/skills/retrieve_skill.py
        provides: Integration of route-selection decision flow before orchestration tool execution.
        min_lines: 120
      - path: tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py
        provides: Tests for selector output shape, retry-on-unclassifiable behavior, and conflict tie-break rules.
        min_lines: 80
    key_links:
      - from: src/memmachine/retrieval_agent/skills/retrieve_skill.py
        to: src/memmachine/retrieval_agent/skills/route_policy.py
        via: Top-level runtime resolves route decisions through route-policy contract helpers.
        pattern: route_policy
      - from: tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py
        to: src/memmachine/retrieval_agent/skills/retrieve_skill.py
        via: Route-selection tests exercise retrieve-skill decision contract and retry behavior.
        pattern: RetrieveSkill
---

<objective>
Implement deterministic `select-skill` route decision policy for top-level
orchestration, including confidence/reason contract output and one-retry
resolution semantics.

Purpose: satisfy ROUT-01 and baseline bounded routing behavior for phase safety.
Output: route-policy module + retrieve-skill integration + route-selection tests.
</objective>

<execution_context>
@/memverge/home/tomz/.codex/get-shit-done/workflows/execute-plan.md
@/memverge/home/tomz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-routing-and-fallback-safety/02-CONTEXT.md
@.planning/phases/02-routing-and-fallback-safety/02-RESEARCH.md

@src/memmachine/retrieval_agent/skills/retrieve_skill.py
@src/memmachine/retrieval_agent/agents/tool_select_agent.py
@tests/memmachine/retrieval_agent/test_retrieve_skill_orchestration_loop.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route decision contract and parser</name>
  <files>src/memmachine/retrieval_agent/skills/route_policy.py, src/memmachine/retrieval_agent/skills/types.py</files>
  <action>Create strict route decision models/helpers that normalize selector outputs into deterministic fields: `selected_route`, `confidence_score`, `reason_code`, and optional `reason_note` / `fallback_trigger_reason`. Include validation for confidence range and allowed route names.</action>
  <verify><automated>.venv/bin/pytest tests/memmachine/retrieval_agent/test_route_policy.py -k "contract or parse or confidence"</automated></verify>
  <done>Route policy module exists with strict parser/validation and test coverage for contract behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate selector retry and conflict tie-break into RetrieveSkill</name>
  <files>src/memmachine/retrieval_agent/skills/retrieve_skill.py, src/memmachine/retrieval_agent/skills/specs/top_level/retrieve_skill.md</files>
  <action>Update top-level runtime to call selector decision flow before orchestration tools, retry once when selector cannot classify, and resolve retry conflicts by higher confidence. Keep decision output deterministic and attach route metadata to metrics/session events.</action>
  <verify><automated>.venv/bin/pytest tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py -k "retry or conflict or selected_route"</automated></verify>
  <done>RetrieveSkill performs model-driven route selection with one retry and deterministic tie-break behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-selection integration tests</name>
  <files>tests/memmachine/retrieval_agent/test_route_policy.py, tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py</files>
  <action>Add focused tests for route output schema, unclassifiable retry behavior, conflicting-route tie-break by confidence, and route metadata emission in metrics.</action>
  <verify><automated>.venv/bin/pytest tests/memmachine/retrieval_agent/test_route_policy.py tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py</automated></verify>
  <done>Route policy and retrieve-skill route-selection semantics are integration-tested and deterministic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv/bin/pytest tests/memmachine/retrieval_agent/test_route_policy.py tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py`
- [ ] `.venv/bin/ruff check src/memmachine/retrieval_agent/skills tests/memmachine/retrieval_agent/test_route_policy.py tests/memmachine/retrieval_agent/test_retrieve_skill_route_selection.py`
- [ ] Route metrics include deterministic decision fields (`selected_route`, `confidence_score`, `reason_code`)
</verification>

<success_criteria>
- All tasks completed
- ROUT-01 contract behavior implemented and tested
- Selector retry/tie-break semantics match phase context decisions
</success_criteria>

<output>
After completion, create .planning/phases/02-routing-and-fallback-safety/02-01-SUMMARY.md
</output>
